import { lazy } from 'react';

// const Page = lazy(() => import('---path---'));

export const routes = {
  // aPage: {
    // title: 'A Page',
    // path: '/page',
    // Component: Page,
  // },
} as const;
